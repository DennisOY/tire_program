<scroll-view class="main_container" scroll-y>
  <block wx:for="{{lookup_cate}}" wx:for-item="lookup" wx:key="item_id">
    <view class="content" id="{{lookup.id}}" bindtap="bindViewTap">
      <text class="text_layout">{{lookup.name}}</text>
    </view>
  </block>
</scroll-view>